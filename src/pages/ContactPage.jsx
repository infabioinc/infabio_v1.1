import { useState } from 'react';
import { Mail, MapPin, Phone, ArrowRight } from 'lucide-react';
import PageHero from '../components/PageHero';

const inputClass = 'w-full rounded-xl border border-white/10 bg-surfaceElevated px-4 py-3.5 font-sans text-sm text-textMain outline-none transition focus:border-brandBlue focus:ring-2 focus:ring-brandBlue/15';

export default function ContactPage() {
  const [sent, setSent] = useState(false);
  const handleSubmit = (event) => {
    event.preventDefault();
    const data = new FormData(event.currentTarget);
    const subject = encodeURIComponent(`Strategy call request from ${data.get('name')}`);
    const body = encodeURIComponent([`Name: ${data.get('name')}`, `Email: ${data.get('email')}`, `Phone: ${data.get('phone') || 'Not provided'}`, `Company: ${data.get('company') || 'Not provided'}`, `Budget: ${data.get('budget')}`, '', data.get('message')].join('\n'));
    setSent(true);
    window.location.href = `mailto:hello@infabio.com?subject=${subject}&body=${body}`;
  };

  return <><PageHero eyebrow="Contact" title="Let’s protect your budget and" accent="grow smarter." description="Tell us where your brand is today and what you want to achieve. We’ll use the first conversation to identify the clearest growth opportunities." image="/bg_hero_2026.png" /><section className="relative pb-28"><div className="container mx-auto grid gap-8 px-6 lg:grid-cols-[0.75fr_1.25fr]"><div className="space-y-5 rounded-2xl border border-white/10 bg-surface p-7 md:p-9"><span className="eyebrow">Talk to us</span><h2 className="font-display text-4xl font-light leading-tight text-textMain">A focused first conversation.</h2><p className="font-sans text-sm font-light leading-7 text-textBody">No generic pitch. We’ll talk through your goals, current marketing, and where budget is being lost or underused.</p><div className="space-y-4 pt-4 font-sans text-sm text-textBody"><a href="mailto:hello@infabio.com" className="flex items-center gap-3 hover:text-textMain"><Mail className="h-5 w-5 text-brandOrange" /> hello@infabio.com</a><div className="flex items-center gap-3"><Phone className="h-5 w-5 text-brandBlue" /> Free strategy consultation</div><div className="flex items-center gap-3"><MapPin className="h-5 w-5 text-brandOrange" /> Gurugram & Jaipur, India</div></div></div><form onSubmit={handleSubmit} className="rounded-2xl border border-white/10 bg-surface p-7 shadow-[0_30px_70px_var(--glass-shadow)] md:p-10"><div className="grid gap-5 md:grid-cols-2"><label className="font-sans text-xs font-semibold uppercase tracking-wider text-textBody">Name *<input className={`${inputClass} mt-2`} name="name" required /></label><label className="font-sans text-xs font-semibold uppercase tracking-wider text-textBody">Work email *<input className={`${inputClass} mt-2`} name="email" type="email" required /></label><label className="font-sans text-xs font-semibold uppercase tracking-wider text-textBody">Phone<input className={`${inputClass} mt-2`} name="phone" type="tel" /></label><label className="font-sans text-xs font-semibold uppercase tracking-wider text-textBody">Company<input className={`${inputClass} mt-2`} name="company" /></label></div><label className="mt-5 block font-sans text-xs font-semibold uppercase tracking-wider text-textBody">Monthly marketing budget *<select className={`${inputClass} mt-2`} name="budget" required defaultValue=""><option value="" disabled>Select a range</option><option>Under ₹1 lakh</option><option>₹1-5 lakh</option><option>₹5-15 lakh</option><option>₹15 lakh+</option></select></label><label className="mt-5 block font-sans text-xs font-semibold uppercase tracking-wider text-textBody">What would you like to improve? *<textarea className={`${inputClass} mt-2 min-h-36 resize-y`} name="message" required /></label><button type="submit" className="btn-primary mt-6 gap-2 text-xs uppercase tracking-widest">Book free strategy call <ArrowRight className="h-4 w-4" /></button>{sent && <p className="mt-4 font-sans text-sm text-textBody">Your email app is opening with the enquiry details ready to send.</p>}</form></div></section></>;
}
